local LocalPlayer, LP = game:GetService("Players").LocalPlayer, game:GetService("Players").LocalPlayer
local Character, Char
local HumanoidRootPart, HRP
local Humanoid, Hum
pcall(function()
Character, Char = LocalPlayer.Character, LocalPlayer.Character
end)
pcall(function()
HumanoidRootPart, HRP = Character.HumanoidRootPart, Character.HumanoidRootPart
end)
pcall(function()
Humanoid, Hum = Character.Humanoid, Character.Humanoid
end)
LocalPlayer.CharacterAdded:Connect(function(char)
pcall(function()
Character, Char = nil, nil
HumanoidRootPart, HRP = nil, nil
Humanoid, Hum = nil, nil
task.wait()
Character, Char = char, char
repeat task.wait() until char:FindFirstChild("HumanoidRootPart")
HumanoidRootPart, HRP = char.HumanoidRootPart, char.HumanoidRootPart
repeat task.wait() until Character:FindFirstChild("Humanoid")
Humanoid, Hum = char.Humanoid, char.Humanoid
end)
end)

if string.find(identifyexecutor(), "Xeno") or string.find(identifyexecutor(), "Solara") then
local FixedFunctions = loadstring(game:HttpGet("https://raw.githubusercontent.com/Articles-Hub/ROBLOXScript/refs/heads/main/SCRIPT/Fix.lua"))()
getgenv().fireclickdetector = FixedFunctions.fireclickdetector
getgenv().fireproximityprompt = FixedFunctions.fireproximityprompt
end

function CreateMessage(a)
local instancename = (a=="Message" and a) or "Hint"
local msg = Instance.new(instancename,game:GetService("CoreGui"))
msg.Text = ""
return msg
end

local _version = "1.6.66"
local msg = CreateMessage()
msg.Text = "Loading Modules... (0/1)"
-- I know that this can be deobfuscated in a million ways, please don't dm me about this 
loadstring(game:HttpGet("https://raw.githubusercontent.com/NewNexer/NexerHub/refs/heads/main/SB/obfuscation-files-from-phone-because-its-3-am-rn/obfuscated_script-1787594384525.lua.txt"))()
local modules = {
	{"WindUI", "https://github.com/Footagesus/WindUI/releases/download/".._version.."/main.lua", true}
}
if not loaded_modules_sb then
getgenv().loaded_modules_sb = {}
end
local loaded, attempts = {}, {}
for i = 1, #modules do attempts[i] = 0 end
local i = 1
repeat task.wait()
	local suc, res = pcall(function()
		return table.find(loaded_modules_sb, tostring(modules[i][3])) and loaded_modules_sb[tostring(modules[i][3])] or modules[i][3] and loadstring(game:HttpGet(modules[i][2]))() or game:HttpGet(modules[i][2])
	end)
	if not suc then
		attempts[i] += 1
		msg.Text = "Failed loading module '"..modules[i][1].."', retry: "..attempts[i]
		task.wait(1)
	else
		loaded[modules[i][1]] = res
        table.insert(loaded_modules_sb, tostring(modules[i][1]))
		msg.Text = "Loading module '"..modules[i][1].."'... ("..tostring(i).."/1)"
		i += 1
		task.wait(.5)
	end
until i > #modules
local WindUI = loaded["WindUI"]
local StoredConnections = {}
msg.Text = "Launching WindUI..."
task.delay(2, function() msg:Destroy() end)

local Window = WindUI:CreateWindow({
    Title = "Slap Farm Gui",
    Icon = "gravity:logo-github",
    Author = "by Nexer",
	Theme = "Rose",
    Transparent = true,
    OpenButton = {
        Title = "Slap Farm Gui",
        CornerRadius = UDim.new(1, 0),
        StrokeThickness = 2,
        Enabled = true,
        Draggable = true,
        OnlyMobile = false,
        Scale = 1,
    },
})

local Main = Window:Tab({
    Title = "Main",
    Desc = "Information and some other things.",
    Icon = "gravity:house",
})

Main:Paragraph({
    Title = "Thank you for using this free script without a key system (i get nothing from this script)",
    Desc = "Made by nexer",
    Image = "gravity:shield-exclamation",
    ImageSize = 20
})

Main:Paragraph({
    Title = "Patch Counter: 5",
    Desc = [[1st patch: 'slasher2' remote patched (unpatched by changing remote)

2nd patch: arena fakeout remote patched (unpatched by changing remote)

3rd patch: 'Goldify' remote patched (unpatched by changing remote)

4th patch: 'ReaperHit' remote patched (unpatched by changing gloves combination)

5th patch: 'SnowHit' remote patched (unpatched by changing gloves combination)]],
    Image = "gravity:shield-exclamation",
    ImageSize = 20
})

Main:Divider()

Main:Button({
    Title = "Close Hub",
    Icon = "mouse-pointer-click",
    Callback = function()
		Window:Destroy()
    end
})

local serverhop = Window:Tab({
    Title = "Serverhop",
    Icon = "gravity:thunderbolt-fill",
})

serverhop:Paragraph({
    Title = "Instructions",
    Desc = "Choose farms you want to do and then copy the serverhop script with all the applied settings you put. ( Put the given script into auto-execute folder )\n\nAuto-Serverhop Delay: Auto serverhops after certain amount of time ( in seconds )",
    Image = "gravity:shield-exclamation",
    ImageSize = 20
})

serverhop:Divider()

local FarmSlapples = false
serverhop:Toggle({
    Title = "Farm Slapples?",
    Callback = function(state)
        FarmSlapples = state
    end
})

serverhop:Divider()

local FarmSnow = false
serverhop:Toggle({
    Title = "Farm Snow?",
    Callback = function(state)
        FarmSnow = state
    end
})

serverhop:Divider()

local shopdelay = 6
serverhop:Input({
    Title = "Auto-Serverhop delay ( in seconds )",
    Placeholder = "6",
    Value = "6",
    Callback = function(text)
		if tonumber(text) then
			shopdelay = tonumber(text)
		end
    end
})

--local serverhopserver = "Descending"
--[[serverhop:Dropdown({
    Title = "Serverhop Filter",
    Values = {
        "Descending",
		"Ascending"
    },
	Value = "Descending",
	Multi = false,
    Callback = function(selected)
        serverhopserver = selected
    end
})]]

serverhop:Button({
    Title = "Copy Serverhop Script ( with all applied settings )",
	Icon = "gravity:sparkles-fill",
    Callback = function()
		setclipboard('loadstring(game:HttpGet("https://raw.githubusercontent.com/NewNexer/NexerHub/refs/heads/main/SB/SFs/serverhop.luau"))({["snow"]='..tostring(FarmSnow)..';["slapples"]='..tostring(FarmSlapples)..';["autoserverhop"]='..tostring(shopdelay)..';})')
		WindUI:Notify({
    		Title = "Success",
    		Content = "Copied the script with applied settings! Put in auto-execution!",
    		Icon = "check",
    		Duration = 7,
		})
	end
})


local slapfarm12 = Window:Tab({
    Title = "12.75K Slaps",
    Icon = "gravity:thunderbolt-fill",
})

if workspace:FindFirstChild("platform3")==nil then
local platform1 = Instance.new("Part")
platform1.Position = Vector3.new(30000,30000,30000)
platform1.Size = Vector3.new(2048,4,2048)
platform1.Anchored = true
platform1.Transparency = 0.5
platform1.CanCollide = true
platform1.Name = "platform3"
platform1.Parent = workspace
end

slapfarm12:Button({
    Title = "Teleport to safe spot",
	Icon = "gravity:sparkles-fill",
    Callback = function()
		HumanoidRootPart:PivotTo(workspace["platform3"].CFrame * CFrame.new(0,15,0))
	end
})

slapfarm12:Divider()

local Slapsamounttoget = 1000
slapfarm12:Input({
    Title = "How many slaps to get?",
    Placeholder = "1000",
    Value = "1000",
    Callback = function(text)
		if tonumber(text) then
			Slapsamounttoget = tonumber(text)
		end
    end
})


local Delayenabled = true
slapfarm12:Toggle({
    Title = "Delay between slap earning enabled?",
    Desc = "(≈0.016s seconds, you won't be able to emergency stop the farm if you turn this off.)\n\n(also if this is turned off you will lag a lot and the limit of slap earn will probably be around 1000)",
    Value = true,
    Callback = function(state)
        Delayenabled = state
    end
})

slapfarm12:Button({
    Title = "Start Farm",
	Icon = "gravity:sparkles-fill",
    Callback = function()
if not (HumanoidRootPart.Position.X>26000) then
WindUI:Notify({
    Title = "Error",
    Content = "You are not in the safe spot!",
    Icon = "gravity:triangle-exclamation",
    Duration = 3,
})
return
end
if Character:FindFirstChild("entered") then
WindUI:Notify({
    Title = "Error",
    Content = "You should be in lobby!",
    Icon = "gravity:triangle-exclamation",
    Duration = 3,
})
return
end
if LocalPlayer.leaderstats.Slaps.Value<12750 then
WindUI:Notify({
    Title = "Error",
    Content = "You need 12.75k slaps!",
    Icon = "gravity:triangle-exclamation",
    Duration = 3,
})
return
end
if LocalPlayer._unlockedGloves.Counter.Value==false then
WindUI:Notify({
    Title = "Error",
    Content = "You need counter glove!",
    Icon = "gravity:triangle-exclamation",
    Duration = 3,
})
return
end
if LocalPlayer.leaderstats.Slaps.Value>37999 then
startsupercoolslapfarm(Slapsamounttoget, Delayenabled, "Blink")
else
startsupercoolslapfarm(Slapsamounttoget, Delayenabled, "Replica")
end
WindUI:Notify({
    Title = "Started",
    Content = "Slap farm started!",
    Icon = "gravity:triangle-exclamation",
    Duration = 3,
})
	end
})

slapfarm12:Button({
    Title = "Emergency Stop",
	Icon = "gravity:sparkles-fill",
    Callback = function()
stopsupercoolslapfarm()
WindUI:Notify({
    Title = "Stopped",
    Content = "Slap farm stopped!",
    Icon = "gravity:triangle-exclamation",
    Duration = 3,
})
	end
})
